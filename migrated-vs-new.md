# TrustBank CBS — Migrated vs. New

What the .NET 10 migration **carried over** from the legacy WebForms/VB.NET application, and what is **new** — architecture, framework, reusable components, technologies, infrastructure and operations.

> **Scope.** This is the *cross-cutting* inventory: platform, framework, components, technologies, architecture. It is deliberately **not** a screen-by-screen or procedure-by-procedure ledger — for that see [migration-estimate/00-SUMMARY.md](migration-estimate/00-SUMMARY.md) (legacy scope counted) and [`TflCbs.Lab/README.md`](../TflCbs.Lab/README.md) (the per-procedure conversion archive).
>
> **Last verified:** 2026-07-30, against the solution source — 36 projects in `TflCbsNet10Sol.slnx`. Markdown is the source of truth; the `.html` twin is generated by [scripts/render-docs-html.py](../scripts/render-docs-html.py).
>
> **Authority.** Where this document and [CLAUDE.md](../CLAUDE.md) disagree, `CLAUDE.md` wins.
>
> **Visual companion:** [architecture/diagrams/](architecture/diagrams/index.html) — the layered architecture (clients → gateway → hosts → screen RCLs → framework → 9 modules → data, with the legacy estate alongside) and a features/technologies/components board. The `.svg` files paste straight into PowerPoint or Word if you need to present any of the tables below.

## How to read this

Every row is tagged with one of three dispositions. The distinction matters commercially: *Ported* is behaviour we must not change, *Rebuilt* is where the shape legitimately differs from legacy, and *New* is capability the bank did not previously have.

| Tag | Meaning |
| --- | --- |
| **Ported** | Legacy behaviour reproduced deliberately faithfully — 1:1, sometimes byte-for-byte. Divergence here is a defect. |
| **Rebuilt** | Same user-facing capability, re-engineered on new foundations. The *shape* changed on purpose; the outcome is equivalent or better. |
| **New** | No legacy counterpart at all. Net addition to the platform. |

---

## 1. At a glance

| | Legacy (as found) | New (.NET 10) |
| --- | --- | --- |
| Runtime | .NET Framework 4.8 | **.NET 10** (`net10.0`, set centrally) |
| Language | VB.NET (`strict="false"`, late binding permitted) | **C#** with `Nullable` + `ImplicitUsings` enabled solution-wide |
| Web stack | ASP.NET **WebForms** — `.aspx` + `.ascx` + MasterPages, ViewState, postbacks | ASP.NET Core **MVC** — controllers, Razor views, attribute routing |
| Deployment unit | One IIS site, one monolithic web app | **36-project modular monolith**; 1 default host + 4 thin per-module hosts + a YARP gateway; IIS **and** Docker |
| Business logic | ~5,317 stored procedures + compiled `Trust.Bank.BusinessLogicLayer.dll` | 9 per-domain **service module assemblies**, `Result`/`Result<T>`, **no new procs / no raw SQL** |
| Data access | Enterprise Library Data + `AdDataAccessComponent.dll`, SQL Server only | **TflOmniDb** — `Repository<T>` + `DataAccess`, async-only, SQL Server · Oracle · PostgreSQL |
| Entities | none (DataSets / loose readers) | **3,682** generated `[DbTable]`/`[DbColumn]` entity classes |
| Reusable UI | 19 `.ascx` controls + `window.showModalDialog` popups | 14 shared partials + 8 `cbs-*.js` behaviours, one in-page AJAX modal |
| Auth | `<authentication mode="None">`, hand-rolled session login | Encrypted **CbsAuth** cookie + server session store + fail-closed middleware + cross-host SSO |
| Automated tests | none found | xUnit service tests · **NetArchTest** boundary suites · in-process integration tests |
| Observability | ad-hoc (`AdDiagnostics.dll`, log files) | **TflOmniLog** (Serilog, category/PII/audit) + **OpenTelemetry** traces & metrics over OTLP |

**Counted legacy scope for context:** 1,458 `.aspx` screens (1,059 functional + 399 reports) · 5,317 stored procedures · 1,497 VB code-behind files · 19 reusable controls.

---

## 2. Architecture

The legacy application had no enforced internal architecture: any page could reach any DLL or call any procedure. Two structural decisions replace that, both **New**, and both compiler-enforced rather than documented-and-hoped-for.

| Concern | Legacy | New | Tag |
| --- | --- | --- | --- |
| Framework/screen separation | none — pages, controls and BLL freely intermixed | **Horizontal split**: `TflCbs.Framework` references only `TflCbs.Abstractions`; screens depend on the framework, never the reverse. Arch-tested. See [architecture/reusable-boundary.md](architecture/reusable-boundary.md) | **New** |
| Business-layer structure | one compiled BLL + 5,317 procedures | **Modular monolith**: 9 per-domain assemblies, module = bounded domain (never per-service). Complete 2026-07-29; the interim single `TflCbsServices` assembly was dissolved. See [architecture/modular-monolith.md](architecture/modular-monolith.md) | **New** |
| Dependency-free foundation | — | `TflCbs.Abstractions`: `Result`/`Result<T>`, `ICbsModule`/`ICoreCbsModule`, the four framework ports, boundary DTOs, and the shared `ServiceQuery`/`DbErrors` helpers. No TflCbs.Entities, no TflOmniDb, no ASP.NET | **New** |
| Cross-module coupling | direct calls | **Published contracts only** — `TflCbs.Modules.General.Contracts` (`IScrollService`, `IProcessBlockCheck`); consumers never see General's implementation | **New** |
| Inversion at the framework boundary | — | **Four ports** (`IBroadcastSource`, `IBankVariableSource`, `IMenuSource`, `ISessionValidator`) implemented by modules, consumed by the framework, with no-op defaults | **New** |
| Screen packaging | `.aspx` files in folders | **Per-domain Razor Class Libraries** (`TflCbs.Modules.<Area>.Web`) composed by the host via gated `AddApplicationPart` | **Rebuilt** |
| Shared master data | duplicated per module | `TflCbs.Modules.Reference` with an explicit **admission rule** (no domain behaviour **and** ≥2 consumers) so it cannot become a dumping ground | **New** |
| Namespace discipline | mixed | **Namespace == assembly**, enforced by `NamespaceDisciplineTests`; zero exceptions remain | **New** |
| Legacy URL continuity | — | A DbMigrator script rewrites the screen's `a_Menus.NavigateURL` from `~/x.aspx` to its new MVC route, so the existing menu entry opens the migrated screen. (Until 2026-08-24 an in-memory config map did this, so the legacy app could keep the old rows.) | **New** (in service of **Ported** behaviour) |
| Screen navigation model | `MainMenu.ascx` reading rights per page | `MenuService` + server-side menu authorization; the menu **is** the access-control list | **Rebuilt** |

Full project-by-project map: [architecture-overview.md](architecture-overview.md).

---

## 3. Framework & web plumbing

Legacy cross-cutting behaviour lived in `Base.master`'s per-request pipeline plus copy-pasted code-behind. It is now one library with a two-call setup: `AddCbsFramework()` + `UseCbsFramework()`.

| Capability | Legacy | New | Tag |
| --- | --- | --- | --- |
| Cross-cutting request pipeline | `MasterPages\Base.master` code-behind, per page | `CbsAccessMiddleware` — one ordered pipeline | **Rebuilt** |
| Access control | per-page rights checks, fail-**open** by omission | **Fail-closed**: a route is allowed only if authenticated + module selected + URL in the user's server-side menu. `[CbsAccessScreen]` binds sub-routes to their owning screen | **Rebuilt** — the *policy* is new (fail-closed) |
| Single-session guard | `IsUserAlreadyLoggedIn` in the master page | Per-request check via `ISessionValidator` → `AuthenticationService.IsSessionActiveAsync` | **Ported** |
| Financial-year readiness gate | master-page check | Per-screen gate, opt out via `[CbsSkipFinancialYearCheck]`; missing FY → `login?reason=financialyearnotfound` with a visible notice | **Ported** (behaviour) / **Rebuilt** (mechanism) |
| CRUD controller scaffolding | copy-pasted code-behind | `CbsScreenController` base — `TryOpenRecord`/`TryBeginWrite`/`TryConsume`/`ProtectId`, `SavedOk`/`UpdatedOk`/`DeletedOk`, `TryLoad`, `EditFailed`, `RunSearch` | **New** |
| Search backend | encrypted SQL shipped in the popup URL | `SearchEndpoint` + `SearchRequest`/`SearchResponse` — **the server owns the query**; only values cross the wire | **Rebuilt** — a security fix, not a port |
| Primary keys on the client | raw PKs in query strings and grid rows | `RowToken` — AES, bound to screen + session, **single-use on write** | **New** |
| No-cache on authenticated responses | inconsistent | Enforced centrally | **Rebuilt** |
| Provider selection | SQL Server, compile-time | `DatabaseSelector` + multi-provider `DataAccess` wiring; dev-only `_DbSwitcher` to change provider at runtime | **New** |
| Health checks | none | `DatabaseHealthCheck` + endpoints per host | **New** |
| Security response headers | none | `UseCbsSecurityHeaders` first in the pipeline — CSP, `X-Frame-Options`, et al. | **New** |
| Forwarded headers | n/a (single site) | `ForwardedHeaders:Enabled` so a host behind the gateway sees the real origin | **New** |
| Notifications | ad-hoc `alert()` / result popups | `Notify` + `UserMessage` → blocking ack modal for server outcomes; `cbsNotify` toast for client guards | **Rebuilt** |
| Antiforgery | ViewState/EventValidation | ASP.NET Core antiforgery, covered by `AntiforgeryTests` | **Rebuilt** |
| Login rate limiting | none (DB lockout counters only) | `cbs-login` policy in `AddCbsFramework`, applied first by `UseCbsFramework` on **every** host — per client IP + path, 10/60 s, 429 + `Retry-After`, on both credential POSTs; the DB lockout policy is kept as the per-account layer | **New** |

---

## 4. Reusable UI components

This is the one area with an existing detailed mapping — [legacy-vs-new-components.md](legacy-vs-new-components.md) gives per-control property tables and usage recipes. Summary:

| Legacy control | New counterpart | Tag |
| --- | --- | --- |
| `TssiplDatePicker.ascx` | `_DatePicker` + `DatePickerModel` + `cbs-datepicker.js` — three dropdowns + a native-calendar button (`showPicker()`), leap-year clamp, ISO hidden field | **Ported** shape, **Rebuilt** internals, calendar **new** |
| `TssiplRecordSelector(.ascx)` V1/V2 + `RecordSelector.aspx` | `_RecordPicker` + `RecordPickerModel` + `cbs-search.js` | **Rebuilt** |
| `TssiplSearchBox.ascx` + `RecordSelector.aspx` grid | `_SearchModal` + `SearchDescriptor`/`SearchColumn`/`SearchFilter` + `SearchEndpoint` + `RowToken` | **Rebuilt** |
| `Account.ascx` + COA/Account selectors + `AccountSelector.aspx` | `_AccountPicker` (3 typed segments) + `AccountPickerModel` + `_AccountSearchModal` + `cbs-account-picker.js` | **Rebuilt** |
| `ActionButtons.ascx` | `_ActionBar` + command-dispatch POST | **Ported** |
| `MainMenu.ascx` + `CollapsablePanelStart/End.ascx` | `_MenuOverlay` / `_MenuGrid` + `MenuService` | **Rebuilt** |
| ad-hoc `alert()` / `MessageBox.aspx` | `_Notification` ack modal + `cbsNotify` toast | **Rebuilt** |
| `Alerts.ascx` | `AlertService` gating + shell surfacing | **Ported** |
| `PasswordPolicy.ascx` | `PasswordPolicyRules` (in `TflCbs.Core.Authentication`) | **Ported** |
| `window.showModalDialog` popups (per control) | **one** shared in-page AJAX modal | **Rebuilt** |

**Components with no legacy counterpart** — all **New**:

| Component | What it does |
| --- | --- |
| `_CommandPalette` + `cbs-command-palette.js` | Ctrl+K quick-access over the session menu, client-side |
| `cbs-form-guard.js` | Unsaved-changes guard + `data-confirm` |
| `cbs-idle.js` + `KeepAliveController` | Idle warning at `Cbs:IdleWarnMinutes`, logout at `IdleLogoutMinutes`, keep-alive ping → 204, closes the `b_UserLogTime` row |
| `cbs-uppercase.js` | Delegated, caret-preserving, IME-aware default-uppercase entry; `cbs-keep-case` opts out |
| `_SessionInfoBar` | Bank/org, working date + session, FY range, last login, screen caption — rendered from session, no per-request DB |
| `_DbSwitcher` | Development-only runtime provider switch |
| `_LayoutBlank` | Chrome-free layout for login/error |
| `/Dev/Components` showcase | Dev-gated page rendering every reusable component with dummy data and all options |
| `RowToken` | Opaque row ids (see §3) |

Client assets are served from the shared RCL at `~/_content/TflCbs.Web.Shared/` — **New** as a packaging mechanism (legacy had loose files under the site root).

---

## 5. Data access, entities & business logic

| Concern | Legacy | New | Tag |
| --- | --- | --- | --- |
| DAL | Enterprise Library Data, `TSSIPL.Practices.EnterpriseLibrary.Data.dll`, `AdDataAccessComponent.dll` | **TflOmniDb** — `Repository<T>` (`GetAsync`/`GetAllAsync`/`Where`/`Count`/`Insert`/`Update`/`Delete`) + `DataAccess.QueryAsync` | **Rebuilt** |
| Database engines | SQL Server | **SQL Server · Oracle · PostgreSQL** — per-provider quoting, param prefixes, identity, paging, dirty-read hints | **New** |
| Sync/async | synchronous | **Async-only**, `CancellationToken` throughout (no sync overloads) | **New** |
| Entities | DataSets / untyped readers | **3,682 generated** partial entity classes, UPPERCASE nullable props, `varchar`→`AnsiString`, nested `Cols` token class per entity | **New** |
| Entity generation | hand-written SQL everywhere | `TflOmniDb.Scaffold` — reverse-engineers live schemas (or an air-gap dump), wizard + CLI, C#/VB, `--sensitive` PII marking, partial classes so hand-written logic survives regeneration | **New** |
| Business logic location | 5,317 stored procedures + compiled BLL | Per-domain service modules; **no new procs, no raw SQL** for new master CRUD | **Rebuilt** |
| Error handling | exceptions / output params / `@error` | **`Result`/`Result<T>`** — no throwing across the service boundary, no silent failures | **New** |
| Transactions | proc-level | `BeginTransactionAsync`, `IUnitOfWork`, `ExecuteInTransactionAsync` with transient-failure retry | **Rebuilt** |
| Concurrency | none observed | Native **row-version** columns auto-increment and guard UPDATEs → `ConcurrencyException` | **New** |
| Large result sets | full materialization | `StreamAllAsync`/`StreamWhereAsync`/`StreamQueryAsync`/`StreamProcedureAsync` | **New** |
| Bulk load | row-by-row | Provider fast paths — `SqlBulkCopy` / PostgreSQL `COPY` / `OracleBulkCopy`, with batching | **New** |
| Procedure support | the primary programming model | `ExecuteProcedureAsync` retained (INPUT/OUTPUT/RETURN, multiple result sets) — for **legacy procs already migrated as procs**, not for new work | **Ported** (capability) |
| Query composition | string-concatenated SQL | LINQ predicates + typed `Cols` tokens + the shared `ServiceQuery` helpers (`Has`/`Eq`/`Val`/`Paged`, page-size bounds) | **Rebuilt** |
| Duplicate/FK error mapping | per-proc `@error` codes | Shared `DbErrors.IsDuplicate`/`IsForeignKey`, provider-aware | **New** |
| Proc-parity proof | — | The `perf` harness in `TflCbs.Lab.Demo` times a legacy T-SQL proc against its migrated C# service across three targets; 12 dated reports in [perf/](perf/) | **New** |

---

## 6. Security, authentication & session

| Concern | Legacy | New | Tag |
| --- | --- | --- | --- |
| Auth model | `<authentication mode="None">` + hand-rolled `Login.aspx` session login | Encrypted **`CbsAuth`** cookie + `CbsSessionTicket` + server-side `CbsSessionStore` | **Rebuilt** |
| Credential check | `pr_g_AuthenticateUser` | `AuthenticationService.AuthenticateUserAsync` (C#) + `LoginRules` + `PasswordPolicyRules` | **Ported** (rules) / **Rebuilt** (implementation) |
| Password storage | legacy reversible/encoded scheme | **PBKDF2 + per-user salt** (`PASSWORDHASH`, `PASSWORDVERSION`), one-off clean-break migration via `TflCbs.Tools.PasswordReset` (`--dry-run` supported) | **Rebuilt** — deliberate break, not a port |
| Crypto library | in-app VB routines | `TflSecurityCrypto` — 1:1 VB.NET port, **byte-for-byte output compatible** (3DES, RC4, salted MD5/SHA1/SHA256/384/512, hardware fingerprint, RNGCSP) | **Ported** |
| Stale-session recovery | — | `b_UserLogTime.LoginInfo` must carry `<REMOTE_HOST>`; `AuthenticateUserAsync` force-logs-out the user's own stale session from the same workstation *before* the "already connected" gate. Parsed only via `LoginInfo` — one sanctioned parser | **Ported** (intent) / **New** (single-parser discipline) |
| Logout bookkeeping | `Authentication.TraceLogoutTime` | `AuthenticationService.TraceLogoutTimeAsync` + `AcctGrp21Service.UserSwitchLogTimeAsync` — closes open `b_UserLogTime` rows | **Ported** |
| Idle policy | ASP.NET session timeout only | **Three clocks that must agree** — client warn/logout (`Cbs:IdleWarnMinutes`/`IdleLogoutMinutes`) and server TTL (`Auth:SessionIdleMinutes`); `CbsSessionOptions.Resolve` **refuses to start** if the server TTL doesn't exceed the client logout | **New** |
| Cross-host login | n/a | **Internal SSO** — one login recognized on every CBS host; shared DataProtection key ring in `CBS_DATAPROTECTIONKEYS`; global logout via `CBS_SESSIONREVOCATIONS`; per-host session rehydration from the cookie ticket. No external IdP/SAML/OIDC | **New** |
| Secrets | connection strings in `Web.Config` | Config/env only — `appsettings.Development.json` is gitignored and `CopyToPublishDirectory=Never`; prod supplies `Database__ConnectionStrings__SqlServer` via env/secret store | **New** |
| Connected-user administration | — | `/Administration/ConnectedUsers` + `ConnectedUserService` | **Rebuilt** |

---

## 7. Cross-cutting platform services

| Capability | Legacy | New | Tag |
| --- | --- | --- | --- |
| Logging | `AdDiagnostics.dll`, Enterprise Library Logging, ad-hoc files | **TflOmniLog** — `ITflLogger`, Serilog-backed, **ASP.NET-free** so the data layer can use it. Category routing + per-category files, config-driven levels hot-swappable via `IOptionsMonitor` | **Rebuilt** |
| Audit trail | per-screen, inconsistent | Audit API (who/what/when/before-after) into its own trail | **Rebuilt** |
| PII protection | none | Recursive PII masking (regex + value patterns) before any sink; `--sensitive`/`[DbColumn]` PII flags drive parameter redaction in the DAL | **New** |
| Correlation | none | Correlation-id enrichment reusing `Activity.Current.Id`; cross-host enrichment (UserId/UserName/SessionId/Host) for multi-host tracing | **New** |
| Log sinks | file | File · console · **batched SQL Server** (`ILogRowWriter` seam, `sql/CbsLog.sql`) · **alert-on-Critical** (webhook/SMTP via `IAlertChannel`, throttled) | **New** |
| Log volume control | none | Token-bucket sampling/rate limiting per level+category+template | **New** |
| Traces & metrics | none | **OpenTelemetry** via `AddCbsObservability` — ASP.NET Core / HTTP / runtime instrumentation, OTLP export to the `grafana/otel-lgtm` dev backend (`compose.observability.yaml`, Grafana :3000). Primer: [observability/observability-primer.md](observability/observability-primer.md) | **New** |
| Caching of master data | per-user session snapshots (`Hashtable`s in session) | Generic **`IReferenceCache`** — category+id keyed, read-through, generation-token flush-all, per-key stampede guard, TTL backstop, **fail-open**; provider from `Cache:Provider` (Memory default / Redis / SQL Server / bring-your-own). Admin screen `/Bank/ReferenceCache`. An admin flush reaches logged-in users on their next request — no re-login | **Rebuilt** — and it unblocked session externalization by removing non-serializable session state |
| Broadcast / marquee | `b_MarqueeAlert` read per page | `GetActiveBroadcastAsync` + `BroadcastService` (2-min cached, once — not per-user-per-request) | **Rebuilt** |

---

## 8. Deployment, hosting & operations

Legacy: one IIS site, manual file copy. New: **four supported shapes**, scripted.

| Concern | Legacy | New | Tag |
| --- | --- | --- | --- |
| Hosting shapes | 1 (IIS single site) | **4** — IIS single-host, IIS multi-host, Docker single-host, Docker multi-host. Driven by [`scripts/deploy-cbs.ps1`](../scripts/deploy-cbs.ps1) via the **cbs-deploy** skill | **New** |
| Gateway | none | **`TflCbs.Gateway`** (YARP, :5100/:7100) — one external origin, path routing to the owning host, health checks, load balancing, global rate limiting. Pure proxy: no framework, no screens, no project references | **New** |
| Thin per-module hosts | none | 4 — Hr (:7300), Lockers (:7200), RetailBanking (:7400), Administration (:7500); each runs **one** domain + the core modules | **New** |
| Containers | none | `Dockerfile` + `compose.yaml` (1 container) / `compose.multi.yaml` (6, only the gateway port published) / `compose.multi.https.yaml` / `compose.observability.yaml` | **New** |
| Package management | `Bin\` full of committed DLLs (~40) | **Central Package Management** — versions only in `Directory.Packages.props`, version-less `PackageReference`; shared defaults in `Directory.Build.props`. Three vendored in-house DLLs remain by `HintPath` (TflOmniDb, TflSecurityCrypto, TflCbs.Entities) | **Rebuilt** |
| Module gating | n/a | `Modules:Enabled` — and `AddCbsModules` **cross-checks the output directory at startup and throws** if a deployed module assembly wasn't passed. Nothing else catches that mistake | **New** |
| Dev cluster startup | n/a | `scripts/start-cbs-hosts.*` / `stop-cbs-hosts.*`; plain `dotnet run` per project also brings the cluster up over HTTPS | **New** |
| Port allocation | one | Documented map: [port-map.md](port-map.md) | **New** |
| Certificates | manual | [deploy/self-signed-cert.md](deploy/self-signed-cert.md); gateway→backend **must** be HTTPS — the failure mode is documented in [deploy/gateway-backend-scheme.md](deploy/gateway-backend-scheme.md) | **New** |
| Config per site | `Web.Config` | Per-host `appsettings` + IIS `web.config` `<environmentVariables>`. ⚠️ `dotnet publish` / `deploy-cbs-iis.ps1` regenerates `web.config` and **wipes** those — re-add after every redeploy | **Rebuilt** |

---

## 9. Technology stack — old → new

| Area | Legacy | New |
| --- | --- | --- |
| Framework | .NET Framework 4.8 | .NET 10 |
| Language | VB.NET | C# (nullable enabled) |
| UI | WebForms, ViewState, postbacks, MasterPages, `.ascx` | ASP.NET Core MVC, Razor, Razor Class Libraries, tag helpers |
| AJAX | `System.Web.Extensions` (ASP.NET AJAX), `ad_ajax_dac.aspx` | `fetch` + JSON endpoints (`cbs-*.js`) |
| Dialogs | `window.showModalDialog` | one shared in-page modal |
| DI | none (`new`) / `Microsoft.Practices.ObjectBuilder` | built-in `Microsoft.Extensions.DependencyInjection`, `ValidateOnBuild` |
| Config | `Web.Config` XML + `App_Code` | `IConfiguration` + options pattern + env vars |
| Data | Enterprise Library Data, `AdDataAccessComponent` | TflOmniDb (`Microsoft.Data.SqlClient` 7.0.1 · `Oracle.ManagedDataAccess.Core` 23.7.0 · `Npgsql` 9.0.2) |
| JSON | `Newtonsoft.Json`, `Ad.Json` | `System.Text.Json` |
| Logging | `AdDiagnostics`, EntLib Logging | TflOmniLog → Serilog 4.2 |
| Telemetry | — | OpenTelemetry 1.17 (hosting, OTLP exporter, ASP.NET Core/HTTP/runtime) |
| Caching | ASP.NET session/cache | `IDistributedCache` — Memory / Redis (`StackExchangeRedis`) / SQL Server |
| Proxy | — | YARP 2.3 |
| Excel/PDF/print | EPPlus, ClosedXML, Spire.XLS, Spire.PDF, Excel interop, `PdfiumViewer`, `AdDosPrinting`, `Interop.OWC10`, `Interop.MSComctlLib` | **not yet migrated** — see §11 |
| Zip | `Ionic.Zip.Reduced`, `ICSharpCode.SharpZipLib` | not yet needed |
| QR / JWT | `QRCoder.dll`, `JWT.dll` | not yet needed (MFA QR is a staged epic) |
| Tests | none | xUnit 2.9 · NetArchTest 1.3 · `Microsoft.AspNetCore.Mvc.Testing` |
| Codegen | none | `TflOmniDb.Scaffold`; Roslyn (`Microsoft.CodeAnalysis.CSharp`) for lab function-wise promotion |

---

## 10. Testing, quality gates & tooling

All **New** — no automated tests were found in the legacy application.

| Gate | What it enforces |
| --- | --- |
| `TflCbs.Tests` (xUnit) | Cross-provider, non-destructive service tests across all nine service modules. Includes `SharedServiceHelperTests`, which **fails the build** on a privately re-declared `ServiceQuery`/`DbErrors` helper |
| `TflCbs.ArchTests` (NetArchTest) | 10 suites: framework ⊥ screens (`BoundaryTests`, `BoundaryGuaranteeTests`), web-module boundaries, namespace == assembly, module config gating, promoted-module list, guard ordering, session-timeout coherence, reference-cache rules, resolve-all-modules |
| `TflCbs.IntegrationTests` (Mvc.Testing) | Boots the **real** host pipeline in-process (`CbsWebAppFactory`) — middleware order included — and asserts the fail-closed guard, antiforgery and authenticated access |
| `TflOmniLog.Tests` | Logging library unit tests |
| `TflCbs.Demo` | Console mirror of every service method against any provider; kept in sync by the **cbs-sync-test-demo** agent |
| `TflCbs.Lab` + `MIGRATION_RECIPE.md` | Every procedure is converted in the lab first, then promoted into its owning module assembly. Golden rule: **no raw SQL** |
| Perf harness | Proves a rewrite is no slower than the procedure it replaced ([perf/](perf/)) |
| Doc tooling | `render-all-docs-html.py` / `render-docs-html.py` / `build-docs.mjs` / `build-arch-doc.mjs` — Markdown is the source of truth, every other format is generated |

Repeatable-delivery assets that did not exist before: the [CRUD-screen tutorial](guides/building-a-crud-screen.md), the [new-module recipe](guides/starting-a-new-module.txt), the [search/RowToken flow reference](guides/search-and-rowtoken-flow.md), and three maintenance subagents (`cbs-sync-test-demo`, `component-demo`, `component-docs-sync`).

---

## 11. Deliberately not carried over, and known gaps

Stated plainly so nobody assumes parity where there is none.

**Replaced on purpose (do not port back)**

- **SQL in the client.** Legacy shipped encrypted SQL in popup URLs. Never reintroduced — the server owns the query.
- **Raw primary keys on the client.** Replaced by `RowToken`.
- **Per-page rights checks.** Replaced by the fail-closed menu-derived guard.
- **Reversible password storage.** Clean break to PBKDF2; every user was reset to a temp password once.
- **ViewState / postback lifecycle.** No equivalent; screens are stateless MVC round trips.
- **Session snapshots of bank/branch variables.** Replaced by the read-through reference cache (this also removed non-serializable session state).
- **New stored procedures.** Prohibited for new master CRUD.
- **Magic-string `command` dispatch as the only POST model.** Now per-action attribute routing; command-dispatch remains where it fits.

**Not yet migrated**

- **Reports** — 399 of the 1,458 legacy screens. No reporting stack chosen yet; the legacy Excel/PDF/DOS-print DLL zoo has no counterpart.
- **The bulk of functional screens** — 1,059 counted; roughly a dozen are live in the new host (State, District, Taluka, DiscipActionHistory, LockerType, AccHoldingAmount, Module, Role, Account, Clients, ReferenceCache, ConnectedUsers) plus the shell (Login, Modules, Home/menu, AccessDenied, Error).
- **Core engine procedures** — interest, GL, day-end, clearing (~600 pd in the estimate).
- **Login extras** — MFA (AD / QR / OTP), RC4 login path, `InitSession` (see [TODO.txt](TODO.txt)).
- **Oracle gap in TflOmniDb** and server-side uppercase normalisation (the current uppercase behaviour is a client convenience only).
- **Data migration + parallel-run tooling.**

Effort and sequencing for the above: [migration-estimate/00-SUMMARY.md](migration-estimate/00-SUMMARY.md). Open risks as put to management: [architecture/TrustBank-CBS-Architecture-Brief.md](architecture/TrustBank-CBS-Architecture-Brief.md) §6. Remediation status: [architecture-review/REMEDIATION-PLAN.md](architecture-review/REMEDIATION-PLAN.md).

---

## 12. Related documents

| Document | Why you'd go there instead |
| --- | --- |
| [architecture/diagrams/](architecture/diagrams/index.html) | The same story as a picture — layered architecture + a features/technologies/components board, as `.svg` (presentation-ready) and 2× `.png` |
| [legacy-vs-new-components.md](legacy-vs-new-components.md) | Property-by-property control mapping and copy-paste recipes — the detail behind §4 |
| [architecture-overview.md](architecture-overview.md) | Every project, its dependencies, composition order and end-to-end request flow |
| [libraries/tflomnidb/README.md](libraries/tflomnidb/README.md) + [api-reference](libraries/tflomnidb/docs/api-reference.md) · [usage-guide](libraries/tflomnidb/docs/usage-guide.md) | Full TflOmniDb surface behind §5 — including [AdDataAccessComponent-vs-TflOmniDb](libraries/tflomnidb/docs/AdDataAccessComponent-vs-TflOmniDb.html) and the [porting cheat-sheet](libraries/tflomnidb/docs/Migration-from-AdDataAccessComponent.md) off the legacy Enterprise-Library DAL |
| [libraries/tflsecuritycrypto/README.md](libraries/tflsecuritycrypto/README.md) + [security-notes](libraries/tflsecuritycrypto/docs/security-notes.md) | Full TflSecurityCrypto surface behind §6 — how the byte-for-byte legacy compatibility is *proven*, and the primitive-by-primitive security posture |
| [architecture/modular-monolith.md](architecture/modular-monolith.md) · [reusable-boundary.md](architecture/reusable-boundary.md) | The decision records behind §2 |
| [FEATURES-SHOWCASE.txt](FEATURES-SHOWCASE.txt) | Flat one-line-per-feature capability list (no migrated/new distinction) |
| [architecture/TrustBank-CBS-Architecture-Brief.md](architecture/TrustBank-CBS-Architecture-Brief.md) | Management-altitude summary with counted figures and open risks |
| [migration-estimate/00-SUMMARY.md](migration-estimate/00-SUMMARY.md) | Legacy scope counted; effort and timeline |
| [README.md](README.md) | The full documentation index |
